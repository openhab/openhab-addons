// Generated by CoffeeScript 1.9.2
'use strict';
angular.module('ZooLib.services.iconResolver', []).factory('iconResolver', function() {
  var ICON_PREFIX;
  ICON_PREFIX = 'icon-';
  return function(item) {
    var i, len, ref, val;
    if ((item != null ? item.tags : void 0) == null) {
      return;
    }
    ref = item.tags;
    for (i = 0, len = ref.length; i < len; i++) {
      val = ref[i];
      if (val.substr(0, ICON_PREFIX.length) === ICON_PREFIX) {
        return val;
      }
    }
  };
});
