// Generated by CoffeeScript 1.9.2
'use strict';
angular.module('ZooLib.directives.magnificPopup', []).directive('magnificPopup', function($compile, $rootScope) {
  var defaults;
  defaults = {
    type: 'inline',
    fixedContentPos: true,
    fixedBgPos: false,
    closeBtnInside: true,
    preloader: false,
    midClick: true,
    removalDelay: 300,
    mainClass: 'my-mfp-slide-bottom'
  };
  $rootScope.closePopup = function() {
    return $.magnificPopup.close();
  };
  return {
    restrict: 'A',
    scope: {},
    link: function($scope, element, attr) {
      attr.callbacks = {
        ajaxContentAdded: function() {
          var content, scope;
          content = this.content;
          scope = content.scope();
          $compile(content)(scope);
          return scope.$digest();
        }
      };
      $scope.closePopup = function() {
        debugger;
      };
      element.magnificPopup(angular.extend({}, defaults, attr));
    }
  };
});
