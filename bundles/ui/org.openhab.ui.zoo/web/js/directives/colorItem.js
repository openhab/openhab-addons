// Generated by CoffeeScript 1.9.1
'use strict';
angular.module('ZooLib.directives.colorItem', []).directive('colorItem', function() {
  return {
    restrict: 'E',
    replace: true,
    template: '<b>To be implemented!</b>',
    scope: {
      item: '='
    },
    link: function(scope, elem, attrs) {
      var i, len, ref, tag;
      scope.options = {};
      ref = scope.item.tags;
      for (i = 0, len = ref.length; i < len; i++) {
        tag = ref[i];
        if (tag === 'power') {
          scope.options.cssIconClass = 'i-power';
        }
        if (tag === 'light') {
          scope.options.cssIconClass = 'i-light-on-small';
        }
      }
    }
  };
});
